//公共配置信息
//var root = "https://uestctest.utools.club";
var root = "https://uestctest.cn1.utools.club"
var rurl = root + "/register"; //注册接口
var lurl = root + "/login";    //登录接口
var indexurl = root + "/index"
var callback = root + "/callback";    //用户反馈接口
var generate = root + "/generate"; //文章列表生成接口
var add_reading_record = root + "/add_reading_record"; //增加阅读记录
var logout = root+"/logout"
